import SwiftUI

struct ErrorView: View {
    var body: some View {
        VStack {
            Text("Connection error!").padding(.horizontal).padding(.vertical, 10)
            Text("Support: tg @swissmer").padding(.horizontal).padding(.vertical, 10)
        }
    }
}

